// NOTE This file is auto-generated by Contentlayer

import { readFileSync } from 'fs'
import { fileURLToPath } from 'url'
import { dirname, join } from 'path'

const __filename = fileURLToPath(import.meta.url)
const __dirname = dirname(__filename)


export { isType } from 'contentlayer2/client'

// NOTE During development Contentlayer imports from `.mjs` files to improve HMR speeds.
// During (production) builds Contentlayer it imports from `.json` files to improve build performance.
const allBlogs = JSON.parse(readFileSync(join(__dirname, 'Blog/_index.json'), 'utf8'))
const allAuthors = JSON.parse(readFileSync(join(__dirname, 'Authors/_index.json'), 'utf8'))

export { allBlogs, allAuthors }

export const allDocuments = [...allBlogs, ...allAuthors]


